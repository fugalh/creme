nifty applescript dialog for dock/quicksilver (self-contained)
icon
real time zones?
persistence across reboot
list upcoming notifications (atq)
and maybe remove them (atrm)
use a config file instead of hacking the script
switch for POSIX timespec
